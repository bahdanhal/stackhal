framework:
  rate_limiter:
    audit:
      policy: fixed_window
      limit: 10
      interval: '1 day'
      cache_pool: 'app.rate_limit_cache'
    contact:
      policy: fixed_window
      limit: 5
      interval: '1 day'
      cache_pool: 'app.rate_limit_cache'
    composer_license:
      policy: fixed_window
      limit: 30
      interval: '1 day'
      cache_pool: 'app.rate_limit_cache'

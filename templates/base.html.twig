{% set tool_context %}{% block tool_name %}{% endblock %}{% endset %}
{% set current_route = app.request.attributes.get('_route')|default('') %}
<!doctype html>
<html lang="{{ app.request ? app.request.getLocale() : 'en' }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta property="og:site_name" content="{{ 'base.brand'|trans }}">
  <meta property="og:type" content="website">
  {% block robots %}<meta name="robots" content="noindex,nofollow">{% endblock %}
  {% block description %}{% endblock %}
  {% block canonical %}{% endblock %}
  <title>{% block title %}{{ 'base.default_title'|trans }}{% endblock %}</title>
  {% block opengraph %}
    <meta property="og:title" content="{{ block('title')|striptags|trim }}">
    <meta property="og:locale" content="{{ app.request ? (app.request.locale == 'pl' ? 'pl_PL' : 'en_US') : 'en_US' }}">
    <meta property="og:locale:alternate" content="{{ app.request ? (app.request.locale == 'pl' ? 'en_US' : 'pl_PL') : 'pl_PL' }}">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ block('title')|striptags|trim }}">
  {% endblock %}
  <meta name="theme-color" content="#090a0f">
  <link rel="icon" href="/favicon.svg?v=2" sizes="any" type="image/svg+xml">
  <link rel="shortcut icon" href="/favicon.svg?v=2" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/favicon.svg?v=2">
  <link rel="stylesheet" href="/app.css">
  <link rel="stylesheet" href="/toolbox.css">
  <link rel="stylesheet" href="/grouped-findings.css">
  <link rel="stylesheet" href="/contact.css">
  <link rel="stylesheet" href="/identity.css">
  <link rel="stylesheet" href="/ui.css">
  <link rel="stylesheet" href="/psa-space-background.css">
  {% block stylesheets %}{% endblock %}
  <link rel="stylesheet" href="/theme-dark.css">
  <link rel="stylesheet" href="/brand-refresh.css">
  <link rel="stylesheet" href="/page-remix.css">
  <script src="/contact.js" defer></script>
  <script src="/psa-space-background.js" defer></script>
  <script type="application/ld+json">{{ {'@context':'https://schema.org','@graph':[{'@type':'WebSite','@id':'https://stackhal.com/#website','url':'https://stackhal.com/','name':'StackHal','inLanguage':['en', 'pl'],'publisher':{'@id':'https://stackhal.com/#stackhal'},'author':{'@id':'https://stackhal.com/#stackhal'},'dateModified':'2026-08-27'}]}|json_encode(constant('JSON_UNESCAPED_SLASHES') b-or constant('JSON_UNESCAPED_UNICODE'))|raw }}</script>
  {% block head_scripts %}{% endblock %}
</head>
<body class="stackhal-site page-{{ current_route|replace({'_': '-'}) }} {% block body_class %}{% endblock %}">
  <header class="site-header">
    <div class="topbar">
      <div class="brand-lockup">
        {% block brand_lockup %}
        <a class="brand" href="{{ path('landing', {_locale: app.request.locale}) }}" aria-label="{{ 'base.home_label'|trans }}">
          <span class="brand-mark">SH</span>
          <span class="brand-name"><span class="brand-name-full">StackHal</span><span class="brand-name-short">StackHal</span></span>
          <span class="brand-signal">Developer utilities</span>
        </a>
        {% endblock %}
      </div>
      <div class="topbar-actions">
        <nav class="primary-nav" aria-label="{{ 'base.navigation'|trans }}">
          <a href="{{ path('landing', {_locale: app.request.locale}) }}">{{ 'common.all_tools'|trans }}</a>
          <a href="{{ path('blog_index') }}">Blog</a>
          <a href="{{ path('geo_home', {_locale: app.request.locale}) }}">{{ 'base.nav.geo'|trans }}</a>
          <a href="{{ path('seo_audit_home', {_locale: app.request.locale}) }}">{{ 'base.nav.seo'|trans }}</a>
        </nav>
        <details class="tool-menu">
          <summary>{{ 'base.nav.more'|trans }}</summary>
          <nav class="tool-menu-panel" aria-label="{{ 'base.nav.more'|trans }}">
            <a href="{{ path('favicon_suite', {_locale: app.request.locale}) }}">{{ 'base.nav.favicon_suite'|trans }}</a>
            <a href="{{ path('cors_sandbox', {_locale: app.request.locale}) }}">{{ 'base.nav.cors_sandbox'|trans }}</a>
            <a href="{{ path('bimi_studio', {_locale: app.request.locale}) }}">{{ 'base.nav.bimi'|trans }}</a>
            <a href="{{ path('domain_inspector_home', {_locale: app.request.locale}) }}">{{ 'base.nav.domain_inspector'|trans }}</a>
            <a href="{{ path('caddy_transpiler', {_locale: app.request.locale}) }}">{{ 'base.nav.caddy_transpiler'|trans }}</a>
            <a href="{{ path('cidr_matrix', {_locale: app.request.locale}) }}">{{ 'base.nav.cidr_matrix'|trans }}</a>
            <a href="{{ path('regex_transpiler', {_locale: app.request.locale}) }}">{{ 'base.nav.regex_transpiler'|trans }}</a>
            <a href="{{ path('dns_dag_tracer', {_locale: app.request.locale}) }}">{{ 'base.nav.dns_dag_tracer'|trans }}</a>
            <a href="{{ path('pkpass_inspector', {_locale: app.request.locale}) }}">{{ 'base.nav.pkpass'|trans }}</a>
            <a href="{{ path('app_links_validator', {_locale: app.request.locale}) }}">{{ 'base.nav.app_links'|trans }}</a>
            <a href="{{ path('ai_studio_extension', {_locale: app.request.locale}) }}">{{ 'base.nav.ai_studio_extension'|trans }}</a>
          </nav>
        </details>
        {% block language_switch %}
          <a class="language-switch" href="{{ path('landing', {_locale: app.request.locale == 'pl' ? 'en' : 'pl'}) }}" hreflang="{{ app.request.locale == 'pl' ? 'en' : 'pl' }}">{{ app.request.locale == 'pl' ? 'EN' : 'PL' }}</a>
        {% endblock %}
      </div>
    </div>
    {% if tool_context|trim is not empty %}
      <div class="tool-context-bar">
        <a href="{{ path('landing', {_locale: app.request.locale}) }}">{{ 'common.all_tools'|trans }}</a>
        <span aria-hidden="true">/</span>
        <strong>{{ tool_context|trim }}</strong>
      </div>
    {% endif %}
  </header>
  {% block ambient_background %}
    {% if not (current_route starts with 'stackhal_admin_') %}
      <canvas class="psa-space-background" aria-hidden="true" role="presentation" data-nosnippet></canvas>
    {% endif %}
  {% endblock %}
  <main>{% block body %}{% endblock %}</main>
  <footer>{{ 'base.footer'|trans }} · <a class="footer-contact" href="mailto:bahdan.hal@hotmail.com">{{ 'base.contact'|trans }}</a><br><span class="copyright">© {{ "now"|date("Y") }} StackHal · <a class="copyright-link" href="https://bahdanhal.pl/">Bahdan Hal</a> · <a class="copyright-link" href="https://ileza.pl/" target="_blank" rel="noopener">IleZa ↗</a></span></footer>
  {% block scripts %}{% endblock %}
</body>
</html>
